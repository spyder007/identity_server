@using spydersoft.Identity.Models.Admin.ClientViewModels
@model ClientGrantTypesViewModel

<form asp-action="Save" asp-route-id="@Model.Id">
    <div class="card">
        <div class="card-header">
            <h3>@ViewData["Title"]</h3>
        </div>
        <div class="card-body">
            @await Html.PartialAsync("_clientNavBar", Model.NavBar)
            <table class="table table-striped">
                <thead class="thead-dark">
                    <tr>
                        <th>@Html.LabelFor(model => model.NewItem.GrantType)</th>
                        <th>Actions</th>
                    </tr>
                </thead>

                @foreach (var clientClaim in Model.ItemsList)
                {
                    <tr>
                        <td>@clientClaim.GrantType</td>
                        <td><a asp-action="Delete" asp-route-id="@clientClaim.Id" asp-route-parentid="@Model.NavBar.Id" title="Delete"><span class="fa fa-trash"></span></a></td>
                    </tr>
                }
                <tr>
                    <td>
                        <div class="form-group">
                            <input type="text" class="form-control" id="txtClientId" asp-for="NewItem.GrantType" pf-validation-for="NewItem.GrantType" pf-validationerror-class="is-invalid" />
                            <span asp-validation-for="NewItem.GrantType" class="invalid-feedback"></span>
                        </div>
                    </td>
                    <td>
                        <div class="form-group">
                            <input type="submit" value="Add" class="btn btn-primary" />
                        </div>
                    </td>

                </tr>
            </table>
        </div>
    </div>
</form>
