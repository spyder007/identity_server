@using Spydersoft.Identity.Models.Admin.IdentityResourceViewModels
@model IdentityResourcePropertiesViewModel
<section>
    <div class="container-fluid">
        <div class="title-wrapper pt-30">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <div class="title mb-30">
                        <h2>@ViewData["Title"]</h2>
                    </div>
                </div>
                <!-- end col -->
            </div>
            <!-- end row -->
        </div>
        <div class="row">
            <div class="card-style mb-30">
                <form asp-action="Save" asp-route-id="@Model.Id">

                    @await Html.PartialAsync("_identityResourceNavBar", Model.NavBar)
                    <table class="table" aria-label="Identity Resource Properties">
                        <thead class="thead-dark">
                            <tr>
                                <th>@Html.LabelFor(model => model.NewItem.Key)</th>
                                <th>@Html.LabelFor(model => model.NewItem.Value)</th>
                                <th>Actions</th>
                            </tr>
                        </thead>

                        @foreach (var identityResource in Model.ItemsList)
                        {
                            <tr>
                                <td>@identityResource.Key</td>
                                <td>@identityResource.Value</td>
                                <td><a asp-action="Delete" asp-route-id="@identityResource.Id" asp-route-parentid="@Model.NavBar.Id" title="Delete"><span class="fa fa-trash"></span></a></td>
                            </tr>
                        }
                        <tr>
                            <td>
                                <div class="mb-3">
                                    <input type="text" class="form-control" id="txtKey" asp-for="NewItem.Key" pf-validation-for="NewItem.Key" pf-validationerror-class="is-invalid" />
                                    <span asp-validation-for="NewItem.Key" class="invalid-feedback"></span>
                                </div>
                            </td>
                            <td>
                                <div class="mb-3">
                                    <input type="text" class="form-control" id="txtValue" asp-for="NewItem.Value" pf-validation-for="NewItem.Value" pf-validationerror-class="is-invalid" />
                                    <span asp-validation-for="NewItem.Value" class="invalid-feedback"></span>
                                </div>
                            </td>
                            <td>
                                <div class="mb-3">
                                    <input type="submit" value="Add" class="btn btn-sm btn-primary" />
                                </div>
                            </td>

                        </tr>
                    </table>
                </form>
            </div>
        </div>
    </div>
</section>