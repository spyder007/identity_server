apiVersion: v2
name: identity
description: Spydersoft Identity — Duende IdentityServer (main), admin REST API, and admin UI (BFF + React SPA), published as an OCI chart.

type: application

version: 0.1.0

appVersion: "0.0.0"

dependencies:
- name: common
  version: 4.4.0
  repository: https://bjw-s-labs.github.io/helm-charts
